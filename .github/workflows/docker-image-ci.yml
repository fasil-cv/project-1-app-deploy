name: Docker CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  # Build the image
  build:
    runs-on: ubuntu-latest  
    steps:
      - uses: actions/checkout@v4

      - name: build
        run: docker build -t fasilcv/myapp:local .
      - run: docker save -o myapp.tar fasilcv/myapp:local
  # Test the image
  test1-docker-run:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run tests"
  test2:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "test-2"
  test3:
    needs: test2
    runs-on: ubuntu-latest
    steps:
      - run: echo "test-3"

